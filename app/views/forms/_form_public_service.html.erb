<%= simple_form_for(@public_service) do |f| %>
  <%= f.input :name %>
  <%= f.input :position %>
  <%= f.input :phone %>
  <%= f.input :email %>
  <%= f.submit t('form.button.submit'), class: 'btn btn-primary'%>
<% end %>